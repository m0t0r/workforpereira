# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# `pnpm db:check` writes this for the length of the drift check and deletes it in a `finally`.
# It survives only a killed run.
packages/db/.db-check.config.ts

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Impeccable — transient state. `DESIGN.md` and `.impeccable/design.json` are the
# committed artifacts; everything else here is a local cache or a live session log.
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/config.local.json
.impeccable/live/
